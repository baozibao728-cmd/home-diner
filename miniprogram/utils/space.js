// ============================================================
// 情侣空间 · spaceId 的唯一出口
// ------------------------------------------------------------
// 整个项目里所有读写数据的地方都从这里拿 spaceId，
// 云数据库的每条数据也都带 spaceId 字段。
//
// 现阶段只有我们一对情侣，写死为 'default'。
// ★ 未来开放给其他情侣时，只需要改这里：
//   1. 做一个「邀请码配对」页面，配对成功后把分配到的 spaceId
//      存进本地缓存（wx.setStorageSync('spaceId', xxx)）
//   2. getSpaceId() 改成读缓存返回
//   业务页面和云函数的调用方式完全不用动。
// ============================================================

const DEFAULT_SPACE_ID = 'default'

function getSpaceId() {
  return DEFAULT_SPACE_ID
}

module.exports = { getSpaceId }
